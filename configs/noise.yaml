robot:
  type: planar_2r
  link_1: 1.0
  link_2: 0.75

dataset:
  samples: 50000
  noise_std: 0.01

model:
  hidden_dims: [128, 128, 64]

training:
  epochs: 50

reproducibility:
  seed: 42

evaluation:
  noise_levels: [0.0, 0.001, 0.005, 0.01, 0.02, 0.05]
