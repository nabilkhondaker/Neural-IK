# Reproducibility

- All RNG seeds are configurable.
- Dataset generation is deterministic given a seed.
- Configurations are YAML files under `configs/`.
- Experiment outputs include summary JSON and checkpoints.
- `pyproject.toml` pins dependency ranges.
